cache/
web/node_modules/
web/.next/

# --- personal tax data: NEVER in version control (history goes public at the AGPL flip) ---
data/
inputs/
out/
output/
*.pdf
!forms/templates/**/*.pdf   # blank official IRS templates only (public-domain US gov works)
*.p12
*.key

# --- tuned ingestion prompts: real *.txt live in the private telos-ops repo ---
# only the *.txt.example stubs are committed here (public AGPL); the tuned
# prompts are loaded at runtime and must never be committed.
src/telos/ingest/prompts/*.txt

# secrets / env
.env
.env.*
!.env.example

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/
*.egg-info/

# os / editor
.DS_Store
.idea/
.vscode/
.coverage
coverage.xml
htmlcov/
