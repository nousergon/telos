# Telos dashboard — local dev / box deploy (telos-ops hydrates on merge).

# Path to the telos TaxProjection artifact (produced by `python -m telos.planning`).
TAX_PROJECTION_PATH=../cache/tax_projection.json

# Metron read models for investment gains (downstream consumer — M0 contract).
METRON_API_URL=http://localhost:8000
METRON_TENANT_ID=
METRON_PORTFOLIO_ID=
