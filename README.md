# badges

Machine-written. This branch holds shields.io `endpoint` documents published by
`scripts/publish_coverage_badge.sh` on every push to `main` (repository-baseline-policy.md
§5.1) — never edit `coverage.json` by hand.
